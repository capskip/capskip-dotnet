# Normalize line endings to LF in the repository for consistent diffs across OSes.
* text=auto eol=lf

# Keep Windows-oriented tooling files as CRLF.
*.sln text eol=crlf

# Treat binary assets as binary (no line-ending conversion).
*.png binary
